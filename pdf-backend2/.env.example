# Browserless WebSocket endpoint for PDF generation
# For production, use browserless.io or your own browserless instance
BROWSER_WS_ENDPOINT=wss://chrome.browserless.io?token=your_token_here

# Server configuration
PORT=3001
NODE_ENV=production

# Optional: For local development, leave BROWSER_WS_ENDPOINT unset to use local Chrome 